"""Danu: a contour editor and the elevation pipeline behind it.

`core` through `cli` are importable without Qt and run headless. `ui` is the
editor and is the only part which imports PySide6.
"""

__version__ = "0.0.0"
